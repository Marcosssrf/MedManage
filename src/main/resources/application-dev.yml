spring:
  application:
    name: sistemaClinica
  datasource:
    url: jdbc:postgresql://localhost:5433/clinica
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update